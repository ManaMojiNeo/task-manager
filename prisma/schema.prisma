generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

enum TeamRole {
  owner
  team_lead
  member
}

enum InvitationStatus {
  pending
  accepted
  declined
  expired
}
